컬렉션 프레임워크


프레임 워크
	
	인터페이스나 클래스를 준비해놓고 사용하는 것
		ㄴ 상수
		ㄴ 추상메서드

	List
		순서가 있다
		중복이 가능하다
		넣는대로 들어간다

	ArrayList 
	
		List 인터페이스를 구현한 대표적인 클래스
		조회네는 유리하나 추가/삭제에는 불합리하다

	Set
		순서가 없다
		중복이 안된다

	HashSet
	
		Set인터페이스를 구현한 대표적인 클래스
	
	반복자

		Set같은 인덱스가 없는 컬렉션에서 데이터를 하나씩 꺼내줄 수 있는 방법
		반복자 객체ㅡㄴ 컬렉 객체로부터 iterator()메서드를 사용해 생성이 가능하다
			List<integer> list = new ArrayIist()

	hasNext()

		다음에 출력한 요소가 있으면 true, 없으면 false

		결과로  t/f로 나온다는 것은 조건식(if), 반복문의 조건에 들어갈 수 있다

		향상된 for문도 내부에서 반복자를 기반으로 동작을 한다


Collections 클래스

	컬렉션을 사용하는 데 있어 유용한 기능들을 가지고 있는 클래스
	직접 객체를 생성하지 않는다
	몯느 메서드는 static으로 선언이 되어있다
	데이털르 저장하기 보다는 가공/처리하는역할

	정렬 : sort()
	뒤집기 : reverse()
	섞기 : shuffle(()
	최대값 : max
	최소값 min