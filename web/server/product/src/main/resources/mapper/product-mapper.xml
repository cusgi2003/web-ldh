<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace : 현재 Mapper XML이 어떤 Java 인터페이스와 연결되는지 지정 -->
<mapper namespace="com.korea.product.dao.ProductDAO">

	<!-- 전체 조회 -->
	<select id="findAll" resultType="ProductVO">
		SELECT * FROM product
	</select>

	<!-- 상품 등록 -->
	<insert id="createProduct">
		INSERT INTO product (
			product_name,
			product_price,
			product_stock
		) VALUES (
			#{productName},  <!-- #{product_name}에서 변경 -->
			#{productPrice}, <!-- #{product_price}에서 변경 -->
			#{productStock}  <!-- #{product_stock}에서 변경 -->
		)
	</insert>
	
	<!-- 특정 상품 조회 -->
	<select id="findById" resultType="ProductVO">
		SELECT * FROM product 
		WHERE product_id = #{productId}
	</select>

	<!-- 상품 업데이트 -->
	<update id="updateProduct">
		UPDATE product SET
			product_name = #{productName},
			product_stock = #{productStock},
			product_price = #{productPrice}
		WHERE product_id = #{productId}
	</update>
	
	<!-- 상품 수정 -->
	<update id="decreaseStock">
		update prodcut set
			product_stock = #{productStock}
		where product_id = #{productId}
	</update>

</mapper>
